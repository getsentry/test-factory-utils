# TODO: use *-retain storage classes when recreating these PVCs
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
