apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zookeeper-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kafka-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
#      storage: { customer.kafka.disk_size }}
# FIXME
      storage: 400Gi
